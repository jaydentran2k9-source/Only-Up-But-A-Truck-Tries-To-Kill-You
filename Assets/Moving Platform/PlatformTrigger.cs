using UnityEngine;

public class PlatformTrigger : MonoBehaviour
{
    // Drag your platform here in the Inspector
    [SerializeField] private WaypointPath _path;
    [SerializeField] private PlatformMovement _movement;

    // Call this via a button or an event to make the platform move again
    
}
